# Carousel — Vue 3 + Vite

Quick start:

1. Install dependencies:

```bash
npm install
```

2. Start dev server:

```bash
npm run dev
```

3. Build for production:

```bash
npm run build
```

Notes: This project uses Vue 3 and Vite. Replace `src/App.vue` with your carousel implementation.
